<%= form_with(model: lesson_status, local: true) do |form| %>
  <% if lesson_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lesson_status.errors.count, "error") %> prohibited this lesson status from being saved:</h2>

      <ul>
      <% lesson_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if lesson_status.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', lesson_status, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if lesson_status.persisted? %>
      <%= link_to "Cancel", lesson_status, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", lesson_statuses_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
