<%= form_with(model: course_status, local: true) do |form| %>
  <% if course_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_status.errors.count, "error") %> prohibited this course status from being saved:</h2>

      <ul>
      <% course_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_field :description, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if course_status.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', course_status, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if course_status.persisted? %>
      <%= link_to "Cancel", course_status, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", course_statuses_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
